# employee-auth-frontend
React (Vite) based employee authentication frontend implementing signup, login, and email OTP verification. Uses private routing to secure dashboard access, Axios for API integration, and localStorage for session persistence. Features responsive professional UI, reusable components, and logout flow enforcing re-authentication.
